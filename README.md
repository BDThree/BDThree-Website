## bdthree-portfolio

Static personal site for `bdthree.ca`, deployed via Cloudflare Pages.

### Local preview

Open `index.html` directly, or run a tiny local server:

```bash
python3 -m http.server 8787
```

Then visit `http://localhost:8787`.

